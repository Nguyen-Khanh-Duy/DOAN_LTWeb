@model PagedList.IPagedList<QUANLYNHASACH.Models.tbDAILY>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Nhà Sách";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>

    <link href="~/Content/PagedList.css" rel="stylesheet" />
</head>
<body>
    <style>
        h2 {
            background-color: rgb(222, 230, 222);
            padding: 20px;
        }
    </style>
    <center><h2>BẢNG ĐẠI LÝ</h2></center>
    <center>
        <br /><br />
        <table class="table" border="1">
            <tr>
                <th>
                    MaDaiLy
                </th>
                <th>
                    TenDaiLy
                </th>
                <th>
                    TenChuDaiLy
                </th>
                <th>
                    DiaChi
                </th>
                <th>
                    SoDienThoai
                </th>
                <th>
                    @Html.ActionLink("Thêm Mới", "Create") |
                    @Html.ActionLink("Tìm Kiếm", "Find")
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.MaDaiLy)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenDaiLy)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenChuDaiLy)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DiaChi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SoDienThoai)
                    </td>
                    <td>
                        @Html.ActionLink("Sửa", "Edit", new { id = item.MaDaiLy }) |
                        @Html.ActionLink("Chi Tiết", "Details", new { id = item.MaDaiLy }) |
                        @Html.ActionLink("Xóa", "Delete", new { id = item.MaDaiLy })
                    </td>
                </tr>
            }

        </table>
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
    </center>
</body>
</html>
